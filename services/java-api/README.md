# Java API Service Plan

`services/java-api` 是后续 Java 后端预留目录。

建议技术栈：

- Spring Boot 3。
- Java 21 或 Java 17。
- PostgreSQL。
- Redis。
- 对象存储。

职责：

- 微信公众号 / 小程序 / 企业微信身份绑定。
- 简历上传和解析任务。
- Agent 编排接口。
- 人才画像和岗位匹配数据存储。
- HR 导出包生成。

当前阶段先不初始化 Java 工程，等 mobile 前端流程稳定后再创建。
